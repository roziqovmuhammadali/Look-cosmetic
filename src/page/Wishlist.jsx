import Head from "../components/Head";
import CartItems from "../components/CartItems";

const Wishlist = () => {
  return (
    <div>
      <Head />
      <CartItems />
    </div>
  );
};

export default Wishlist;
